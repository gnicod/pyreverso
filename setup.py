from setuptools import setup

setup(name='pyreverso',
      version='0.1',
      description='wip reverso wrapper',
      url='http://github.com/gnicod/reverso',
      license='MIT',
      packages=['pyreverso'],
      install_requires=[
          'bs4',
      ],
      zip_safe=False)
